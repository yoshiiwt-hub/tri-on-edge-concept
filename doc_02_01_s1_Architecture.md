# Tri-On-Edge Stream Architecture（アーキテクチャ基本方針）

　本書は、Tri-On-Edge Stream（生産現場ストリームデータ処理基盤）のシステムアーキテクチャについて、基本方針を示す。

　ただし、本方針自体について、最新の技術動向を注視し評価しながら、実践適用・評価・改善を継続的に繰り返し、育てていく。（Trial）

　また、技術（手段）ありきにならず、ウィル（どうしたいか、ありたい姿、あるべき姿）→ルール（明文化された決まり、標準）→ツール（手段、技術、実行方法）の順序で考える。（Will 1st, Rule 2nd, Tool Later）

- Ver: 03.a.20260814
- By: IWATA,Y.
- Mod: ドキュメント再編

---

対象読者
- 開発者

---

上位文書
- Tri-On-Edge Development Concept（開発基本方針）

---

## 1. ベース技術の選定

### 1. 開発言語

　開発言語として **Python**を使用する

#### 選定理由

　以下より、Trial、Trinityによる改善サイクルを高速・円滑に回すことができるため。

  - Web上で広く情報が公開されており、情報収集が容易。そのため、対応可能な人材・ベンダが豊富。さらに、AIを活用したコード・ドキュメント生成が効果的に行える
  - スクリプトのみで実行可能なインタプリタ言語であり、実装・検証・修正のサイクルを高速で回すことができる
  - 多種多様な外部ライブラリがOSSとして提供されており、生産現場の様々な通信方式・N/Wプロトコルに効率的に対応できる
  - クロスプラットフォームに対応しているため、生産現場の様々なH/W、OS（PLC、ボックスPC、サーバ）で稼働できる

#### Python バージョン、Bit数

- Python 3.11 (64bit) 以上を基本とする。ただし、既存環境によっては Python 3.10 、32bit を許容する。

  - 組込み機器（設備）を含めた、生産現場にある多種の環境に対応するため

~~~text
python = ">=3.10" # 64bit recommended. 32bit allowed as last resort 
~~~

  - OS： Windows / Linux
  - CPUアーキテクチャ： Intel / ARM
  - CPUビット数： 64bit / 32bit

#### 留意点
  - Pythonはインタプリタ言語であるため、演算性能、メモリ領域の厳密な管理による安定稼働には課題がある（GIL:Global Interpreter Lock）
  - 実行環境へのデプロイについては、C言語変換等の技術を用い、演算処理性能・メモリ領域管理に対応すること（後述の『3. 実行環境（ビルド・デプロイ）』を参照）


### 2. システム内データ保持

#### ファイル（設定ファイル）

- **JSON (.json)** を使用する
  - Python標準ライブラリで扱うことができ、Python辞書型との親和性が高いため
  - デファクトの１つであり、エディタなどの対応ツールも豊富であるため

#### システム内部DB
  
- **SQLite** を使用する
  - Python標準ライブラリで扱うことができ、単一ファイルのシンプルな構成で使用できるため
  - WALモードによりRead/Write並列で処理待ちを抑制するなど、性能を考慮すること
  - Journal、TemporaryのMEMORY置き、同期の緩和（OSバッファに任せる）など、ストレージ書込み寿命を考慮すること
  
#### AIモデル入出力・推論実行

- **ONNX** 、 **ONNX Runtime** を使う。
- ただし、MT法・重回帰分析など、人が見て管理できる程度のパラメータで実行可能なものは、設定ファイルとして管理してよい。


### 3. システム内データ交換

#### プロセス内通信
  
- Python標準ライブラリ **queue.Queue** を使用する
  - メッセージキューイングにより、非同期でのリソース効率が高いデータ処理が可能なため
  - データ欠損の発生を抑制できるため

#### プロセス間通信
  
- **Socket通信** を使用する
  - Python環境があれば、追加ライブラリ等は不要で、シンプルな構成で使用できるため

- 性能または堅牢性が求められる場合は Python外部ライブラリ **ZeroMQ (ipc://  tcp://)** を使用する
  - Brokerlessのシンプルな構成で、軽量かつ堅牢な通信を行えるため

#### データ交換形式
  
- **JSON** または **pickle** を使用する。 **（TBD: 将来的に MessagePack を検討）** 
  - Python環境があれば、追加ライブラリ等は不要で、シンプルな構成で使用できるため

- ※ ただし、pickleはシステム内かつ通信時に一時的のみ使用可とし、ファイル保存は行わないこと。バージョン非互換、脆弱性の問題があるため


### 4. 外部システムとのデータ交換

- **parquet** を使用する
  - データ型があり、データ型チェックの問題を回避できるため
  - バイナリ形式であり、テキストエンコードの問題を回避できるため
  - 列指向のデータ構造であり、上位側で必要列のみを読込むなど活用がしやすいため

- **CSV** は人に見せる必要がある時に使用可
  - Python環境があれば、追加ライブラリ等は不要で、シンプルな構成で使用できるため
  - デファクトの１つであり、現場に定着しているExcel、メモ帳などのツールで直感的に参照できるため

- **JSON** は構造を管理できる時のみ使用可
  - 例：Jsonスキーマと合わせて外部システムとやり取りする時など
  - 自由な構造のJsonはチェックの労力が多大であるため

- クラウドへのデータ保管時は **Hive-Style** に対応できること
  - ビッグデータの参照性に優れ、AWS、Azure、GCP等の主要クラウド基盤でデファクトとなっているため


### 5. システム管理

- 通知、記録（管理台帳）、ファイル履歴管理・バックアップ・端末配布などについては、現場に定着している、社内の既存環境へ連携することを検討する

例
  - Microsoft Teams（チャット）による通知
  - Microsoft SharePoint（SPO）での管理台帳
  - Gitによるファイルバージョン管理、バックアップ、配布


## 2. ソフトウェア構造
　コアコンセプトである、Trigger（生産現場の変化点の検出）、Trial（現地・現物・現データでの実践・実証）、Trinity（連携）を実現するため、ソフトウェアの構造について以下を定める。

### モジュール（ファイル）の疎結合構造

Tri-On-Edgeは４Ｓ５定の考え方に基づき、「誰が、何のために、どこを触るか」を明確に分離した **疎結合構造** を採用する。これにより「変更・参照のタイミング（業務フェイズ）」、「変更頻度」、「変更者」を最適化する。

- **現場担当者** は、設定ファイルだけ触ればよい
- **トリガー開発者** は、トリガーのモジュールだけ編集すればよい
- **コア開発者** は基盤だけ管理すればよい
- **他システム管理者** は、システム連携するために制御ファイルだけ扱えばよい

#### 1. コア部

- Stream Core および Supervise Base , Operator 等、データパイプラインの幹線となる基盤モジュール
- 現場への配布後は変更不可（変更には開発環境でのリビルドと再配布が必要）

#### 2. トリガー部

- `importlib` で動的ロード（後述）される Python スクリプト
- トライ段階では、現場で直接編集・差し替え可能
- コアのリビルドなしでロジックを変更可能
- ※ ただし、新しい外部ライブラリの追加はコア層のリビルドが必要

#### 3. 設定部

- コア層、トリガー層の動作パラメータを指定（接続先・閾値・保存先・ポーリング間隔など）
- 設定ファイルのみの変更でデプロイ先ごとの差異を吸収

#### 4. 制御部（制御ファイル）

- システム停止、オフライン状態、デバッグモード、等を特定の制御ファイルの有無・更新日時で制御
- GUIやネットワーク接続なしに、ファイル操作だけでシステムを制御可能


### トリガーの動的ロード

　Stream Coreは、入力・収集（Find Trigger）と出力・後処理（Act Trigger）をハードコーディングせず、必要な物を起動時に動的にロードする（トリガーセット）。

- 高速なTry & Error: トリガー開発者は単一のPythonファイル（新規トリガー）だけを作成し、設定ファイルを書き換えるだけで即座に現場でテスト（Trial）が可能
- コアロジックの保護: 新しい入力ソース（例：新たなPLC機器）や出力先（例：新しいクラウドDB）を追加する際、Stream Core などの基盤モジュールを修正する必要がない
- メモリとリソースの最適化: 実行時に必要なトリガーモジュールだけがロードされるため、未使用の重いライブラリによるメモリ圧迫を防ぎ、エッジPCの限られたリソース（ムダ）を効率的に利用

仕組み
- **動的バインディング**（ `importlib` ）：設定ファイル内に記述された「モジュール名」と「関数名」を文字列として受け取り、`importlib.import_module()` と `getattr()` によって該当するワーカー関数をスレッドとして起動

- **設定ファイル駆動**：実行時に trigger_setting_01.json / trigger_setting_02.json から、動作パラメータを読込む


## 3. 実行環境（ビルド・デプロイ）

　ビルド・デプロイ（実行環境への配布）については以下とする。

#### ポータブル環境（ゼロインストール運用）

　Tri-On-Edgeのデプロイについて、ネットワーク・インターネットへの接続を必要とせず、実行環境に所定のファイル・フォルダを保存することで稼働可能にする。

- **現場PCへの環境構築作業・N/W接続が不要**：現場のPCへのインストール作業・オンラインアップデートが不要
- **環境の分離**：対象PCで稼働している他の制御ソフトの環境を汚染・破壊するリスクを最小化
- **高速なリカバリ**：バックアップした「Tri-On-Edgeのフォルダ」を上書きコピーするだけで数分でリカバリ可能

#### ３段階の実行形式： 

　Tri-On-Edgeは、業務フェーズ（開発、トライ、号口）に応じて、最適な実行形式を使い分ける。

- 開発段階
  - WinPython（Windowsの場合）等のポータブルPython環境を使用し、.pyを直接実行する。
  - コードを直接編集・即実行できる。

- 検証（トライ）段階
  - PyInstallerを使用し、パッケージされた.exeを実行する。
  - トリガー（入力、出力）部分を現場で編集・差替えできる。

- 本番（号口）段階
  - Nuitkaを使用し、C言語を経由してビルドされたバイナリモジュールの.exeを実行する。
  - 実行速度、動作の堅牢性・安定性（メモリ管理等）の向上


以上